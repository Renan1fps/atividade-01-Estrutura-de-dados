#include <stdio.h>
#include <stdlib.h>

int main(){

  printf("\nOcupação de memoria de um char %d\n", sizeof(char));
  printf("\nOcupação de memoria de um int %d\n", sizeof(int));
  printf("\nOcupação de memoria de um float %d\n", sizeof(float));
  printf("\nOcupação de memoria de um double %d\n", sizeof(double));
}